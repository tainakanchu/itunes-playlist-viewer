# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# WSL / NTFS metadata
*:Zone.Identifier

# Node
node_modules
dist
dist-ssr

# Tauri / Rust
src-tauri/target
src-tauri/gen/schemas

# Bundled ffmpeg for Windows builds (downloaded in CI, not committed)
src-tauri/ffmpeg.exe

# Build artifacts
*.tsbuildinfo

# Nix
result
result-*

# direnv
.direnv

# Tauri icon CLI side-effects we don't ship (we target desktop only)
src-tauri/icons/android/
src-tauri/icons/ios/
src-tauri/icons/Square*Logo.png
src-tauri/icons/StoreLogo.png
