# Expo + pnpm: hoist deps so Metro/React Native resolution works without symlink issues.
# See https://docs.expo.dev/guides/monorepos/ (pnpm). mobile/ is installed in isolation
# from the root workspace on purpose.
node-linker=hoisted
